import "./bootstrap";
import {
    Datepicker,
    Input,
    Modal,
    Ripple,
    Select,
    Sidenav,
    initTE,
} from "tw-elements";
initTE({ Datepicker, Input, Modal, Ripple, Select, Sidenav });
